# Render 블루프린트 — GitHub 저장소를 연결하면 자동 배포됩니다.
services:
  - type: web
    name: room-of-errors
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    envVars:
      - key: ADMIN_CODE
        sync: false        # Render 대시보드에서 관리자 코드를 직접 입력
